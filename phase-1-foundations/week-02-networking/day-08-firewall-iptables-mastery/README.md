# Day 08 — Firewall & iptables Mastery

> Control every packet entering your server

## What I Built

_Configure iptables rules from scratch: allow SSH, HTTP/S, deny everything else. Implement port forwarding, rate limiting, and logging. Compare with UFW and nftables._

## How to Run

```bash
# Build/Run instructions here
```

## Tasks

- [ ] Flush rules and set default DROP policy\n- [ ] Allow SSH, HTTP, HTTPS inbound\n- [ ] Implement rate limiting for SSH (anti-brute-force)\n- [ ] Set up port forwarding (e.g., 8080 → 80)\n- [ ] Persist rules with iptables-save / netfilter-persistent

## What I Learned

- [Key learning 1]
- [Key learning 2]

## Tools Used

`iptables` · `ufw` · `nftables` · `ss` · `tcpdump`

## Resume Bullet

> _Configured iptables firewall with default-deny, rate limiting, port forwarding, and persistent rule management_
