# Day 04 — Filesystem & Storage Deep Dive

> LVM, mounts, quotas — the real stuff

## What I Built

_Hands-on lab with disk partitioning, LVM volume creation, filesystem mounts, fstab configuration, and disk quota management on a VM._

## How to Run

```bash
# Build/Run instructions here
```

## Tasks

- [ ] Create partitions with fdisk/parted\n- [ ] Set up LVM: PV → VG → LV\n- [ ] Create ext4/xfs filesystems and mount them\n- [ ] Configure persistent mounts in /etc/fstab\n- [ ] Set up user disk quotas with quota tools

## What I Learned

- [Key learning 1]
- [Key learning 2]

## Tools Used

`fdisk` · `lvm` · `mount` · `fstab` · `quota`

## Resume Bullet

> _Managed Linux storage infrastructure: LVM volumes, filesystem creation, persistent mounting, and user quota enforcement_
