# Day 24 — Dockerized CI Runner

> Self-hosted GitLab runner in Docker

## What I Built

_Deploy a self-hosted CI runner inside Docker. Configure Docker-in-Docker (DinD). Run a sample build and push pipeline._

## How to Run

```bash
# Build/Run instructions here
```

## Tasks

- [ ] Deploy GitLab Runner in Docker\n- [ ] Configure DinD for building images in CI\n- [ ] Register runner with your GitLab\n- [ ] Write a pipeline that builds and pushes an image\n- [ ] Add caching for pip/node_modules

## What I Learned

- [Key learning 1]
- [Key learning 2]

## Tools Used

`docker` · `gitlab-runner` · `dind` · `gitlab-ci`

## Resume Bullet

> _Configured self-hosted GitLab Runner with Docker-in-Docker; built and pushed images through automated CI pipeline_
